# Hyper-Local Intelligence

Opportunity analysis is evidence-gated. The engine can report geographic service gaps only when provider evidence and nearest-provider distance are both available with valid public source metadata.

The result exposes the evidence, source dataset, date, geographic scope, methodology, confidence, and limitations. AI explanations are validated against those structured facts and fall back to a deterministic explanation when the generated text is unsupported or unavailable.